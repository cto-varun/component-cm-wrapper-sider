import React from 'react';
import { Layout, Input, Space, Badge, Menu, Dropdown } from 'antd';
import {
    BellFilled,
    DownOutlined,
    MenuUnfoldOutlined,
    SearchOutlined,
} from '@ant-design/icons';

import './styles.css';

const { Sider } = Layout;

export default function ComponentCmWrapperSiderComponent() {
    return <div className="cm-wrapper-sider" width={48} />;
}
